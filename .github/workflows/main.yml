# This is a basic workflow to help you get started with Actions

name: CI

# Controls when the action will run.
on:
  # Triggers the workflow on push or pull request events but only for the master branch
  push:
    branches:
      - master
      - PANDORA_remaster
      - cleanup_install
  pull_request:
    branches:
      - master
      - PANDORA_remaster

  # Allows you to run this workflow manually from the Actions tab
  workflow_dispatch:

# A workflow run is made up of one or more jobs that can run sequentially or in parallel
jobs:
  # This workflow contains a single job called "build"
  build:
    # The type of runner that the job will run on
    runs-on: ubuntu-latest

    # Steps represent a sequence of tasks that will be executed as part of the job
    steps:
      # Checks-out your repository under $GITHUB_WORKSPACE, so your job can access it
      - uses: actions/checkout@v2

      # Runs a single command using the runners shell
      - name: Set up Python 3.8
        uses: actions/setup-python@v1
        with:
          python-version: 3.8
      - name: Set up directories
        run: python test/install_git_test.py
      - name: Install package
        run: |
          python -m pip install --upgrade pip
          pip install .[test]

      # /opt/hostedtoolcache/Python/3.8.8/x64/lib/python3.8/site-packages
#      - name: Run a multi-line script
#        run: |
#          echo test,
#          pip show modeller
#
#          cd /opt/hostedtoolcache/Python/3.8.8/x64
#          ls -la
#          echo 1
#          cd /opt/hostedtoolcache/Python/3.8.8/x64/bin
#          ls -la
#          echo 2
#          cd /opt/hostedtoolcache/Python/3.8.8/x64/lib
#          ls -la

      - name: Test with pytest
        run: pytest